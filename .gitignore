# Dépendances — réinstallables avec « npm install » (18 Mo)
node_modules/

# Artefacts macOS
.DS_Store

# Sauvegarde d'avant découpage du monolithe : git assure déjà le rollback, et
# ce fichier est identique octet pour octet à vart.html. Exclu pour éviter de
# versionner un doublon de 911 Ko. scripts/split.js le régénère au besoin.
_baseline/

# Verrou de scripts/resplit.js (empêche deux découpages simultanés de se
# marcher dessus sur src/manifest.json)
.resplit.lock

# Ancêtre Kast v2.1 : porte encore l'ancien branding (« Renaud Dékode ») et le
# lien vers la bibliothèque communautaire supprimée. Conserve-le en local,
# mais ne le publie pas : ce serait ré-exposer ce que le rebranding VYT a retiré.
kast v2.1.html
